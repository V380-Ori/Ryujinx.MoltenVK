From 06bbd4710eee78196b09d3e73fb936c2a6e9207b Mon Sep 17 00:00:00 2001
From: dttdrv <dttdrv@users.noreply.github.com>
Date: Tue, 28 Jul 2026 11:21:22 +0300
Subject: [PATCH 1/3] Avoid repeated state finalization for indexed indirect
 draws

---
 MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
index 9289d0c7b..104c49ce0 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
+++ b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
@@ -1418,9 +1418,10 @@ static void bindIndirectZeroDivisorVertexBuffers(MVKCommandEncoder* cmdEncoder,
                 cmdEncoder->beginMetalRenderPass(kMVKCommandUseRestartSubpass);
             }
 
-	        cmdEncoder->finalizeDrawState(stage);	// Ensure all updated state has been submitted to Metal
-
-			if ( !pipeline->hasValidMTLPipelineStates() ) { return; }	// Abort if this pipeline stage could not be compiled.
+			if (drawIdx == 0 || pipeline->isTessellationPipeline() || vtxAdjmts.needsAdjustment()) {
+				cmdEncoder->finalizeDrawState(stage);
+				if ( !pipeline->hasValidMTLPipelineStates() ) { return; }
+			}
 
             switch (stage) {
                 case kMVKGraphicsStageVertex:

From 6cff4397f3eae0353a58328923e05575cb23b24e Mon Sep 17 00:00:00 2001
From: dttdrv <dttdrv@users.noreply.github.com>
Date: Tue, 28 Jul 2026 17:49:14 +0300
Subject: [PATCH 2/3] Keep indexed indirect pipeline validation

---
 MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
index 104c49ce0..078607fbc 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
+++ b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
@@ -1420,8 +1420,8 @@ static void bindIndirectZeroDivisorVertexBuffers(MVKCommandEncoder* cmdEncoder,
 
 			if (drawIdx == 0 || pipeline->isTessellationPipeline() || vtxAdjmts.needsAdjustment()) {
 				cmdEncoder->finalizeDrawState(stage);
-				if ( !pipeline->hasValidMTLPipelineStates() ) { return; }
 			}
+			if ( !pipeline->hasValidMTLPipelineStates() ) { return; }
 
             switch (stage) {
                 case kMVKGraphicsStageVertex:

From 1d2fd982e2f43f8d7b8866e559b1c37e1f2fdf90 Mon Sep 17 00:00:00 2001
From: dttdrv <dttdrv@users.noreply.github.com>
Date: Thu, 30 Jul 2026 19:20:34 +0100
Subject: [PATCH 3/3] Restore indexed indirect comments

---
 MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
index 078607fbc..fec572cc2 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
+++ b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
@@ -1419,9 +1419,9 @@ static void bindIndirectZeroDivisorVertexBuffers(MVKCommandEncoder* cmdEncoder,
             }
 
 			if (drawIdx == 0 || pipeline->isTessellationPipeline() || vtxAdjmts.needsAdjustment()) {
-				cmdEncoder->finalizeDrawState(stage);
+				cmdEncoder->finalizeDrawState(stage);	// Ensure all updated state has been submitted to Metal
 			}
-			if ( !pipeline->hasValidMTLPipelineStates() ) { return; }
+			if ( !pipeline->hasValidMTLPipelineStates() ) { return; }	// Abort if this pipeline stage could not be compiled.
 
             switch (stage) {
                 case kMVKGraphicsStageVertex:
